package com.example.delogica.config.errors;

public enum ErrorCode {
    VALIDATION_ERROR,
    RESOURCE_NOT_FOUND,
    CONFLICT,
    FORBIDDEN,
    INTERNAL_ERROR,
    BAD_REQUEST,
    JWT_INVALID,
    JWT_EXPIRED,
    JWT_MISSING,
    UNAUTHORIZED

}
